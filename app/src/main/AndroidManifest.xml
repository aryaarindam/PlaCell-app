<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.placell">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_placell"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_placell_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InsertNewsfeed"></activity>
        <activity android:name=".InsertOngoing" />
        <activity android:name=".EditProfile" />
        <activity android:name=".ViewOngoing" />
        <activity android:name=".InsertStory" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>